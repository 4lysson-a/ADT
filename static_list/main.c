#include "li_estatica.h"

int main(){
	setlocale(LC_ALL,"Portuguese");

	printf("Funcionou :)\n");

	// Lista *li;	
	
	// li = cria_lista();

	// printf("\nO tamanho da lista é: %d", tamanho_lista(li));

	// Aluno dados_aluno;
	// strcpy(dados_aluno.nome, "Alysson");
	// dados_aluno.matricula = 10848470;  

	// // insere_lista_final(li, dados_aluno);

	// insere_lista_inicio(li, dados_aluno);


	// libera_lista(li);

	return 0;
}